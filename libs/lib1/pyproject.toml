[tool.coverage.run]
branch = true
source = [ "lib1" ]

[tool.coverage.report]
exclude_lines = [ "if TYPE_CHECKING:" ]
show_missing = true

[tool.pytest.ini_options]
addopts = "--cov --cov-report html:'../../coverage/libs/lib1/html' --cov-report xml:'../../coverage/libs/lib1/coverage.xml' --html='../../reports/libs/lib1/unittests/html/index.html' --junitxml='../../reports/libs/lib1/unittests/junit.xml'"

[tool.poetry]
name = "lib1"
version = "1.0.0"
description = "Automatically generated by Nx."
authors = [ ]
license = "Proprietary"
readme = "README.md"
group = { }

  [[tool.poetry.packages]]
  include = "lib1"

  [tool.poetry.dependencies]
  python = ">=3.9,<3.11"

[tool.ruff]
exclude = [ ".ruff_cache", ".svn", ".tox", ".venv", "dist" ]
line-length = 88
indent-width = 4

  [tool.ruff.lint]
  select = [ "E", "F", "UP", "B", "SIM", "I" ]
  ignore = [ ]
  fixable = [ "ALL" ]
  unfixable = [ ]

[build-system]
requires = [ "poetry-core" ]
build-backend = "poetry.core.masonry.api"
